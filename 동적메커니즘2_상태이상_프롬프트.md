# 동적 메커니즘 PR-C — 상태이상 (턴 종료 데미지)

Antigravity 작업 지시서. PR-A가 만든 ON_TURN_END post 슬롯을 그대로 재사용해, 턴 종료마다 % 데미지를 주는 상태이상(독·화상류)을 추가한다. 새 post 키 `ON_STATUS_TICK`과 액션 `_act_status_tick`을 더하고, step2 부착 expander에 상태이상 입력을 추가한다. 회복(Leftovers)과 독립적으로 작동하며, 사용자가 활성하지 않으면 no-op이라 회귀가 없다.

## 변경 범위

`modules/engine.py` 4곳, `modules/step2_system_definition.py` 1곳. 다른 파일·영역은 건드리지 않는다. game_config 저장은 기존 `_gc["mechanisms"] = _mech_cfg` 병합이 그대로 status를 실어 나르므로 추가 변경이 없다.

## 적용 규칙

- FIND를 찾아 REPLACE로 그대로 교체한다. 들여쓰기·공백·한글 한 글자도 바꾸지 않는다.
- 임의 리팩터·이름 변경·하드코딩 금지. 지시된 치환만.
- 적용 후 `python -c "import modules.engine"` 및 step2 py_compile이 통과해야 한다.

---

# 파일: `modules/engine.py`

## ENG-1 _POST_LEVEL_KEYS에 ON_STATUS_TICK 추가

**FIND:**

```python
# 캐릭터 턴 종료 후 1회 실행되는 동적 메커니즘 hook (타겟 무관)
_POST_LEVEL_KEYS = {"ON_TURN_END"}
```

**REPLACE:**

```python
# 캐릭터 턴 종료 후 1회 실행되는 동적 메커니즘 hook (타겟 무관)
_POST_LEVEL_KEYS = {"ON_TURN_END", "ON_STATUS_TICK"}
```

## ENG-2 _act_status_tick 함수 삽입

**FIND:**

```python
# ── 디폴트 액션을 글로벌 레지스트리에 등록 ──
```

**REPLACE:**

```python
def _act_status_tick(ctx):
    """상태이상 턴 종료 데미지 (독·화상류) — game_config['mechanisms']['status'] 기반.
    부착 캐릭터가 턴 종료 시 vital 자원의 percent만큼 데미지를 받는다. HP는 0 미만으로
    내려가지 않는다. 미부착/미설정/사망 시 no-op."""
    char = ctx["active_char"]
    mechs = (ctx.get("game_config") or {}).get("mechanisms") or {}
    spec = mechs.get("status")
    if not spec:
        return
    col = spec.get("gimmick_col")
    want = str(spec.get("match_value", "")).strip().lower()
    have = str(char.get("gimmicks", {}).get(col, "")).strip().lower() if col else ""
    if not col or have != want:
        return
    percent = float(spec.get("percent", 0.125))
    rm = ctx.get("resource_module")
    vitals = rm.vital_resources() if rm else ()
    rname = vitals[0] if vitals else "HP"
    res = char.get("resources", {}).get(rname)
    if not res or res.get("current", 0) <= 0:
        return
    before = res["current"]
    res["current"] = max(0, res["current"] - res["max"] * percent)
    lost = before - res["current"]
    if lost > 0:
        ctx["add_log"](
            f"  -> [Phase: ON_STATUS_TICK] {char.get('id','?')} {rname} {int(lost)} 상태이상 피해 "
            f"({int(res['current'])}/{int(res['max'])})"
        )
    if res["current"] <= 0:
        ctx["add_log"](f"  [Phase: ON_STATUS_TICK] ☠️ {char.get('id','?')} 상태이상으로 쓰러짐!")


# ── 디폴트 액션을 글로벌 레지스트리에 등록 ──
```

## ENG-3 ON_STATUS_TICK 레지스트리 등록

**FIND:**

```python
DEFAULT_ACTION_REGISTRY.register("ON_TURN_END",   _act_turn_end_heal)
```

**REPLACE:**

```python
DEFAULT_ACTION_REGISTRY.register("ON_TURN_END",   _act_turn_end_heal)
DEFAULT_ACTION_REGISTRY.register("ON_STATUS_TICK", _act_status_tick)
```

## ENG-4 ON_STATUS_TICK 자동 삽입

**FIND:**

```python
    # ON_TURN_END 자동 삽입 — 흐름 끝, 캐릭터 턴 종료 동적 메커니즘 hook
    if "ON_TURN_END" not in [k for k, _ in all_actions]:
        all_actions.append(("ON_TURN_END", "Turn End Effects (턴 종료 효과)"))
```

**REPLACE:**

```python
    # ON_TURN_END 자동 삽입 — 흐름 끝, 캐릭터 턴 종료 동적 메커니즘 hook
    if "ON_TURN_END" not in [k for k, _ in all_actions]:
        all_actions.append(("ON_TURN_END", "Turn End Effects (턴 종료 효과)"))

    # ON_STATUS_TICK 자동 삽입 — ON_TURN_END 직후, 턴 종료 상태이상 데미지 hook
    if "ON_STATUS_TICK" not in [k for k, _ in all_actions]:
        all_actions.append(("ON_STATUS_TICK", "Status Tick (상태이상 처리)"))
```

---

# 파일: `modules/step2_system_definition.py`

## S-1 상태이상 부착 UI를 expander에 추가

**FIND:**

```python
                else:
                    st.warning("기믹 컬럼이 없어 부착 기준을 지정할 수 없습니다.")

        st.divider()
```

**REPLACE:**

```python
                else:
                    st.warning("기믹 컬럼이 없어 부착 기준을 지정할 수 없습니다.")

            _st_on = st.checkbox("상태이상 (턴 종료 % 데미지) 활성", value=False, key="ui_mech_status_on")
            if _st_on:
                if gimmicks:
                    _st_col = st.selectbox(
                        "상태이상 기준 기믹 컬럼", list(gimmicks), key="ui_mech_status_col",
                        help="이 컬럼의 값이 아래 '상태이상 값'과 일치하는 캐릭터가 턴 종료마다 데미지를 받음."
                    )
                    _st_vals = (sorted(df[_st_col].dropna().astype(str).unique().tolist())
                                if _st_col in df.columns else [])
                    if _st_vals:
                        _st_match = st.selectbox("상태이상 값", _st_vals, key="ui_mech_status_val")
                    else:
                        _st_match = st.text_input("상태이상 값", value="Poison", key="ui_mech_status_val_txt")
                    _st_pct = st.number_input(
                        "턴 종료 데미지율 (max HP 대비)", min_value=0.0, max_value=1.0,
                        value=0.125, step=0.01, format="%.4f", key="ui_mech_status_pct"
                    )
                    _mech_cfg["status"] = {
                        "gimmick_col": _st_col,
                        "match_value": str(_st_match),
                        "percent": float(_st_pct),
                    }
                else:
                    st.warning("기믹 컬럼이 없어 상태이상 기준을 지정할 수 없습니다.")

        st.divider()
```

---

## 적용 후 검증 (Antigravity가 보고할 것)

1. `modules/engine.py` import 통과, `modules/step2_system_definition.py` py_compile 통과.
2. `grep -n 'def _act_status_tick' modules/engine.py` → 1건.
3. `grep -n 'register("ON_STATUS_TICK"' modules/engine.py` → 1건.
4. `grep -c 'ON_STATUS_TICK' modules/engine.py` → 4건(_POST_LEVEL_KEYS·함수참조·등록·자동삽입+조건).
5. `grep -c '_mech_cfg\["status"\]' modules/step2_system_definition.py` → 1건.
6. Streamlit Step 2의 "🍃 동적 메커니즘 부착" expander 안에 "상태이상 (턴 종료 % 데미지) 활성" 체크박스가 보이는지.

## 라이브 동작 확인 (사용자)

Step 2에서 상태이상을 활성하고 기믹 컬럼·값(예: status 컬럼 == Poison)·데미지율 0.125를 지정한 뒤, Step 5에서 단일 전투를 돌리면 부착 캐릭터의 턴 종료마다 "[Phase: ON_STATUS_TICK] … 상태이상 피해 (현재/최대)" 줄이 찍힌다. Leftovers(회복)와 동시에 부착하면 회복→상태이상 순으로 둘 다 발화한다.
